# 版本号管理脚本

## 概述

本项目使用统一的版本号管理系统，通过 `Version` 文件控制所有文件中的版本号。

## 使用方法

### 1. 更新版本号

只需修改项目根目录下的 `Version` 文件：

```
0.1.3-dev
```

### 2. 运行更新脚本

在项目根目录执行：

```bash
npm run version:update
```

### 3. 自动更新的文件

脚本会自动更新以下文件中的版本号：

- ✅ `package.json` - npm 包版本
- ✅ `package-lock.json` - npm 锁定文件版本
- ✅ `src-tauri/Cargo.toml` - Rust 包版本
- ✅ `src-tauri/Cargo.lock` - Rust 锁定文件版本
- ✅ `src/views/Home.vue` - 主页显示版本（带 `v` 前缀）
- ✅ `src/views/Settings.vue` - 设置页显示版本（带 `v` 前缀）

## 版本号格式

- **Version 文件格式**: `0.1.2-dev`（不带 `v` 前缀）
- **Vue 文件显示**: `v0.1.2-dev`（自动添加 `v` 前缀）
- **其他文件**: `0.1.2-dev`（不带前缀）

## 注意事项

1. **Version 文件**: 只需包含版本号本身，不要添加 `v` 前缀
2. **Cargo.lock**: 如果脚本更新失败，在下次 Rust 构建时会自动重新生成
3. **提交代码**: 更新版本后记得提交所有修改的文件

## 示例工作流

```bash
# 1. 修改 Version 文件
echo "0.2.0" > Version

# 2. 运行更新脚本
npm run version:update

# 3. 提交更改
git add .
git commit -m "chore: bump version to 0.2.0"
git tag v0.2.0
git push origin main --tags
```

## 脚本输出示例

```
🚀 开始更新版本号...

📖 读取版本号: 0.1.2-dev
✅ 更新 package.json: 0.1.2-dev
✅ 更新 package-lock.json: 0.1.2-dev
✅ 更新 Cargo.toml: 0.1.2-dev
✅ 更新 Cargo.lock: 0.1.2-dev
✅ 更新 Home.vue: v0.1.2-dev
✅ 更新 Settings.vue: v0.1.2-dev

✨ 版本号更新完成！
📌 当前版本: 0.1.2-dev

💡 提示: 如果修改了 Cargo.toml，请运行 "npm run tauri build" 或 "cargo build" 来更新 Cargo.lock
```

## 故障排除

### 问题：脚本执行失败

**解决方案**:
- 确保在项目根目录执行命令
- 检查 `Version` 文件是否存在且格式正确
- 确保所有目标文件都存在且有写入权限

### 问题：Cargo.lock 更新失败

**解决方案**:
- 不用担心，Cargo.lock 会在下次 Rust 构建时自动更新
- 或者手动运行: `cd src-tauri && cargo build`
